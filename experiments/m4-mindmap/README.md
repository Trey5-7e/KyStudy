# M4 思维导图验收样本

这些文件仅包含自建的结构化标题，用于验证 M4 的本地导入草案，不包含题库、课程或个人资料。

- `fixtures/408-knowledge.opml`：8 个节点的 OPML 树；
- `fixtures/network.mm`：7 个节点的 FreeMind 树；
- `fixtures/doctype-rejected.opml`：包含 DTD 的拒绝样本，只用于确认安全边界。

导入流程始终是“资料库上传 → 思维导图刷新资料与草案 → 生成预览草案 → 用户确认或拒绝”。
